-- Table: public.inventory_beekeepers

-- DROP TABLE IF EXISTS public.inventory_beekeepers;

CREATE TABLE IF NOT EXISTS public.inventory_beekeepers
(
    id integer NOT NULL DEFAULT nextval('inventory_beekeepers_id_seq'::regclass),
    name text COLLATE pg_catalog."default" NOT NULL,
    description text COLLATE pg_catalog."default" NOT NULL,
    price money NOT NULL,
    photo bytea[],
    CONSTRAINT inventory_beekeepers_pkey PRIMARY KEY (id)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.inventory_beekeepers
    OWNER to root;